# --- 开发环境 / IDE 相关 ---
.hbuilderx/       # HBuilderX 自动生成的目录
.idea/            # JetBrains IDE (如 WebStorm) 配置
.vscode/          # VS Code 配置
.DS_Store         # macOS 系统文件

# --- 依赖目录 ---
node_modules/     # Node.js 依赖包
unpackage/        # UniApp 打包生成的临时目录（HBuilderX 生成）
dist/             # 构建输出的目录（CLI 生成）

# --- 环境变量 ---
.env.local        # 本地环境变量
.env.*.local      # 其他环境变量（如 .env.development.local）

# --- 日志 / 缓存 ---
*.log             # 日志文件
.cache/           # 缓存目录

# --- 操作系统 / 编辑器临时文件 ---
Thumbs.db         # Windows 缩略图缓存
*.sublime-*       # Sublime Text 临时文件
*.swp             # Vim 交换文件

# --- UniApp 特定 ---
project.config.json  # 微信小程序项目配置（通常需忽略，因开发者不同）
project.private.config.json # 微信小程序私有配置